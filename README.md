### (WIP) utility tools for the thesis:

Isomorphism Interface which currently supports the following edge flips ismophisms:
  - planar triangulation of regular polygons (implemented as DCEL) [operation: edge flip] <-> binary tree [operation: node rotation]
    
Tool for exhaustive search of the simultaneous flip graph space.

(Visualization based on PyGame)
